<Window x:Class="OpenThings.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ResultsWindow" AllowsTransparency="True" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Height="304" WindowStyle="None" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignWidth="335" SizeToContent="Height" Width="500" WindowStartupLocation="Manual" Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False" Left="-2" Top="49">
    <DockPanel Name="dockPanel1" Background="Transparent">
        <ListBox Name="listBox1" Background="Transparent" BorderBrush="Transparent">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="#6C000000" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Padding" Value="10,2,0,2" />
                    <Setter Property="FontSize" Value="22" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </DockPanel>
</Window>
